# 更新日志 / Changelog

所有版本均为单文件、无外部依赖的 HTML deck 能力迭代。版本号形如 `v1.x`，对应 deck 产物内的 `<meta name="deck-version">`。

## v1.7 — 2026-09-18
- 新增**指定页码跳转**：`G` 键 / 点击底部页码 / 直接键数字（多位数字 1 秒后自动跳，Enter 立即跳）。
- 新增**跳页面板**：居中输入框 + 1..N 页码网格 + 目标页标题实时预览 + 越界自动钳制 + `←→` 微调。
- 新增 `scripts/patch_goto.js`：按骨架 GOTO 标记提取能力块，**幂等**注入旧 deck。
- 错误库新增 M025（瞬态 UI 截图需持续触发定时器）、M026（骨架升级后旧 deck 的同步方式）。

## v1.6 — 2026-09-18
- **双向居中定稿**：`transform:scale` + 宽度补偿取代 zoom 方案；`.slide` 上下 padding 严格对称。
- 引入**自动版本号**：`deck_version.json` 注册表 + `bump_version.py`，版本号写入 deck 三处。
- 几何校验修正坐标换算（屏幕坐标 → 画布坐标必须先减偏移再除缩放）。

## v1.5 — 2026-09-18
- 20 页逐页截图目检通过。

## v1.4 — 2026-09-18
- 修复放大后左右裁切。

## v1.3 — 2026-09-18
- 修复页内大片空白（内容自动撑满）。

## v1.2 — 2026-09-18
- 配色语义化重构：`data-tone` 六板块色 + 涨跌色仅用于真实涨跌 + 对比度提升。
- 新增 `scripts/check_color.html` 配色校验（含 `:root` 是否真被解析的检测）。

## v1.1 — 2026-09-18
- 改为固定 16:9 画布 + 三段骨架结构。

## v1.0 — 2026-09-18
- 首版翻页 deck。

---

## 错误库 / Mistake library

`mistaken_data.json` 目前收录 **26 条**真实踩坑（M001–M026），覆盖布局、配色、图表、数据、校验、工程、需求七类。详见该文件与 `README.md`。
